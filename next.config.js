/** @type {import('next').NextConfig} */
const nextConfig = {
  // Enable asset uploading to Vercel
  asset: true,
  // Enable CSS modules
  css: {
    modules: true,
  },
}

module.exports = nextConfig
